<odoo>
  <template id="infinitepay_payment_template" inherit_id="website_sale.payment">
    <xpath expr="//div[contains(@class,'payment_method')][1]" position="before">
      <div class="payment_method">
        <label>
          <input type="radio" name="payment_method" value="infinitepay" class="infinitepay_radio"/>
          Pagar com InfinitePay
        </label>
      </div>
    </xpath>
  </template>

  <template id="infinitepay_payment_js" inherit_id="website_sale.checkout">
    <xpath expr="//form[@id='checkout_form']" position="inside">
      <script type="text/javascript">
        (function () {
          document.addEventListener('DOMContentLoaded', function () {
            var form = document.getElementById('checkout_form');
            if (!form) { return; }
            form.addEventListener('submit', function (ev) {
              var sel = document.querySelector('input[name="payment_method"]:checked');
              if (sel && sel.value === 'infinitepay') {
                ev.preventDefault();
                window.location.href = '/shop/infinitepay';
              }
            });
          });
        })();
      </script>
    </xpath>
  </template>
</odoo>
